import 'package:flutter/material.dart';

class CowInventory extends StatelessWidget {
  const CowInventory({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Text("Cow inventory"),
    );
  }
}
