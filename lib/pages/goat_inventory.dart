import 'package:flutter/material.dart';

class GoatInventory extends StatelessWidget {
  const GoatInventory({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Text("Goat inventory"),
    );
  }
}
